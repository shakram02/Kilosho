import unittest


class TestTrace(unittest.TestCase):
    def test_tracing(self):
        # input_stream = ['int', 'id', ';', 'id', 'assign', 'num', ';', 'if', '(', 'id', 'relop', 'num', ')', '{', 'id',
        #                 'assign', 'num', ';', '}']
        # accept = trace(input_stream, 'grammar_reader/grammar_test_2.txt')
        # self.assertTrue(accept)
        pass
